# ── Git / VCS ──
.git
.gitignore

# ── Python 캐시 ──
__pycache__
*.py[cod]
*.pyo
vulnscan/__pycache__
vulnscan/**/__pycache__

# ── 가상환경 ──
env/
venv/
.venv/

# ── 메인 DB (스캔 결과 = 환경마다 다름) ──
vulnscan.db

# ── NVD/보안 캐시는 이미지에 포함됨 (옵션 B) ──
# nvd_cache.db, kev_cache.json 등은 빌드에 포함되어 이미지 크기 증가
# 대신 초기 실행 시 즉시 사용 가능

# ── 환경 변수 ──
# .env는 .env.example에서 생성되므로 제외
# 실제 .env 파일이 있으면 docker-compose.yml의 env_file로 주입됨
.env.local

# ── 기타 불필요 파일 ──
.cvss_migration_done
server.log
*.log
.claude/

# ── Docker 자체 파일 ──
Dockerfile
docker-compose.yml
.dockerignore

# ── 이전 스크립트 ──
start.sh
update_epss_kev.py
